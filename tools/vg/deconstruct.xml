<tool id="vg_deconstruct" name="vg deconstruct" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>VCF projection of a variation graph against a reference path</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <xrefs>
        <xref type="bio.tools">vg</xref>
    </xrefs>
    <expand macro="requirements"/>
    <version_command>vg version 2>&amp;1 | head -1</version_command>
    <command detect_errors="aggressive"><![CDATA[
#if str($graph.ext) in ('gfa1', 'gfa1.gz')
    ## vg deconstruct cannot read GFA directly; convert to vg first
    #if str($graph.ext) == 'gfa1.gz'
        gunzip -c '$graph' > input.gfa &&
    #else
        ln -s '$graph' input.gfa &&
    #end if
    vg convert -g -p input.gfa > input.vg &&
    #set $graph_arg = 'input.vg'
#else
    ln -s '$graph' 'input.${graph.ext}' &&
    #set $graph_arg = 'input.' + str($graph.ext)
#end if

vg deconstruct
    --threads \${GALAXY_SLOTS:-1}
    #if str($ref_mode.mode) == "path"
        #for $p in str($ref_mode.path).strip().split(',')
            --path '$p.strip()'
        #end for
    #else
        #for $p in str($ref_mode.path_prefix).strip().split(',')
            --path-prefix '$p.strip()'
        #end for
    #end if
    $all_snarls
    $contig_only_ref
    '$graph_arg'
    > '$output_vcf'
    ]]></command>
    <inputs>
        <param argument="--graph" type="data" format="vg,xg,gfa1,gfa1.gz" label="Input graph"
               help="Variation graph in any vg-readable format (vg/xg/GFA). PanSN-named paths recommended for -P prefix mode."/>
        <conditional name="ref_mode">
            <param name="mode" type="select" label="Reference selection">
                <option value="path">Specific path name(s) (-p, comma-separated)</option>
                <option value="prefix" selected="true">PanSN-style path prefix (-P, comma-separated)</option>
            </param>
            <when value="path">
                <param argument="--path" type="text" value=""
                       label="Reference path names"
                       help="Comma-separated path names to deconstruct against, e.g. 'chr1,chr2'.">
                    <validator type="empty_field"/>
                </param>
            </when>
            <when value="prefix">
                <param argument="--path-prefix" type="text" value=""
                       label="Reference path prefix(es)"
                       help="Comma-separated PanSN prefixes, e.g. 'GCA_900093555.2' for the Pv PvP01 reference. All paths starting with this prefix are treated as reference.">
                    <validator type="empty_field"/>
                </param>
            </when>
        </conditional>
        <param argument="--all-snarls" type="boolean" truevalue="--all-snarls" falsevalue=""
               checked="false" label="Report all nested snarls (-a)"
               help="By default only top-level snarls are reported."/>
        <param argument="--contig-only-ref" type="boolean"
               truevalue="--contig-only-ref" falsevalue="" checked="false"
               label="Use CONTIG-only reference names (-C)"/>
    </inputs>
    <outputs>
        <data name="output_vcf" format="vcf" label="${tool.name} on ${on_string}"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="graph" value="simple_2path.gfa" ftype="gfa1"/>
            <conditional name="ref_mode">
                <param name="mode" value="path"/>
                <param name="path" value="ref"/>
            </conditional>
            <output name="output_vcf">
                <assert_contents>
                    <has_line_matching expression="^##fileformat=VCF.+"/>
                    <has_text text="#CHROM"/>
                </assert_contents>
            </output>
        </test>
        <test expect_num_outputs="1">
            <param name="graph" value="simple_4path.gfa" ftype="gfa1"/>
            <conditional name="ref_mode">
                <param name="mode" value="prefix"/>
                <param name="path_prefix" value="ref_"/>
            </conditional>
            <output name="output_vcf">
                <assert_contents>
                    <has_line_matching expression="^##fileformat=VCF.+"/>
                    <has_text text="#CHROM"/>
                </assert_contents>
            </output>
            <assert_command>
                <has_text text="--path-prefix"/>
            </assert_command>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
**vg deconstruct** — project a variation graph to VCF.

Walks the snarls of a variation graph relative to a reference path (or
PanSN-prefix path set) and emits a VCF describing each variant.

Standard pangenome VCF projection: for an 8-strain pangenome with PanSN
naming `SAMPLE#1#CHR`, set `--path-prefix GCA_900093555.2` to use the
PvP01 reference and get an 8-sample VCF in PvP01 coordinates.

-----

**Modes**

- `-p` (path): explicit reference path name(s).
- `-P` (path-prefix): all paths whose name begins with the prefix are
  used as reference — the canonical PanSN approach.

Threads from `GALAXY_SLOTS`.
    ]]></help>
    <expand macro="citations"/>
</tool>
