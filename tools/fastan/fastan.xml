<tool id="fastan" name="fastan" version="0.8+galaxy2" profile="23.0">
    <description>FASTAN tandem-repeat arrays as content-annotated BED6</description>
    <!--
      Dependencies come from bioconda. Both of the blockers this wrapper was written
      around have since been resolved upstream:
        * fastan 0.8 (masking-enabled, -m / .1ano) shipped in bioconda-recipes PR #66389,
          merged 2026-06-17; bioconda now offers 0.5 and 0.8.
        * ANOtoBED IS installed by the fastga package; see its build.sh, which installs
          ANOtoBED, BEDtoANO, ANOstat and ANOshow alongside FAtoGDB.
      FAtoGDB and ANOtoBED come from fastga; FasTAN comes from fastan.

      fastga is pinned to 1.5.20260729, NOT 1.5, and the difference is not cosmetic.
      Released 1.5 contains ANOtoBED but cannot READ what FasTAN writes. Upstream
      redefined the ano M record and added a P record in 0a4a4db ("ANO file definition
      change", 2026-04-23), three months after v1.5 was tagged, and no release has
      followed. FASTAN 0.8 emits the new schema, so the ANOtoBED in fastga 1.5 exits
      "Failed to open .1ano file", naming no version, which is what makes this worth
      pinning rather than documenting. bioconda-recipes#68612 added 1.5.20260729.
    -->
    <requirements>
        <requirement type="package" version="0.8">fastan</requirement>
        <requirement type="package" version="1.5.20260729">fastga</requirement>
    </requirements>
    <command detect_errors="exit_code"><![CDATA[
set -o pipefail &&
#if str($input.ext) == 'fasta.gz'
    gunzip -c '$input'
#else
    cat '$input'
#end if
| awk '/^>/{print;next}{print toupper(\$0)}' > in.fa &&
FAtoGDB in.fa gdb || true ;
FasTAN -m -p -T\${GALAXY_SLOTS:-4} gdb &&
ONEview gdb.1ano
| awk -f '$__tool_directory__/ano2bed6.awk'
| LC_ALL=C sort -k1,1 -k2,2n > '$output'
    ]]></command>
    <inputs>
        <param name="input" type="data" format="fasta,fasta.gz" label="Assembly FASTA"/>
    </inputs>
    <outputs>
        <data name="output" format="bed" label="${tool.name} on ${on_string}: tandem arrays (BED6)"/>
    </outputs>
    <help><![CDATA[
**FASTAN** (Gene Myers) — tandem-array finder. Detects tandem repeats and partitions
them into units. Emitted as **BED6**: name = ``u<unit_size>`` (tandem unit length),
score = unit identity (0-1000 grayscale), strand = real orientation. Complementary to
the DUST/low-complexity tracks. Pipeline: ``FAtoGDB`` -> ``FasTAN -m -p`` (.1ano) -> ``ONEview`` +
awk. NOT ``ANOtoBED``, which reports contig-relative coordinates under the scaffold name (see
check_coordinates.py). Input is upper-cased first.

⛔ **COLUMNS 4 AND 5 DO NOT MEAN THE SAME THING AS IN THE dustmasker, windowmasker AND tantan
TRACKS.** Those three pipe through ``lc_classify.py``, which names a SEQUENCE SIGNATURE
(``polyA``, ``(AT)n``, ``lc``) and scores PURITY x 1000. This tool names the repeat UNIT LENGTH
(``u65``, ``u8``) and scores 1000 - 10 x divergence. Both are 0-1000 and higher-is-better, so a
UCSC hub renders all four happily -- but grouping by name, or comparing scores, ACROSS tracks is
mixing two vocabularies and nothing errors.

⛔ **``lc_classify.py`` IS ABSENT ON PURPOSE.** It brute-forces periods 1-6 only and returns
``lc`` for anything longer. Measured on 103,341 real intervals (ASM1303036v1), **75.9% have a
unit longer than 6** -- 65,084 of them 20 bp or more, median interval 62 bp, longest 22,755.
FASTAN measures the unit from its own tandem-array model (the ``L`` record) and reports
divergence (``X``); classifying that would fail on three quarters of the intervals and replace a
measured value with a guess. The UDT port omits it for the same reason, so the two stay in step.
    ]]></help>
    <citations><citation type="doi">10.1101/2023.04.01.535214</citation></citations>
</tool>
