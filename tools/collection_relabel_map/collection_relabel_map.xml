<tool id="collection_relabel_map" name="Collection relabel map" version="1.0+galaxy0" profile="23.0">
    <description>{a}_{b} TAB {a}.{b} for every ordered pair of element identifiers</description>
    <requirements>
        <requirement type="package" version="3.12">python</requirement>
    </requirements>
    <command detect_errors="exit_code"><![CDATA[
python '$__tool_directory__/relabel_map.py'
    --ids '#echo " ".join([str($el.element_identifier) for $el in $input])#'
    --out '$output'
    ]]></command>
    <inputs>
        <param name="input" type="data_collection" collection_type="list"
               label="List collection"
               help="The ordered-pair relabel map is derived from this collection's element identifiers."/>
    </inputs>
    <outputs>
        <data name="output" format="tabular" label="${tool.name} on ${on_string}: relabel map"/>
    </outputs>
    <help><![CDATA[
Emits a 2-column TSV ``{a}_{b}<TAB>{a}.{b}`` for every ordered pair of element identifiers.
Cross-product cells are named ``A_B`` (the collection-id join separator can't be ``.``);
this map renames them to ``A.B`` (Phase E ids) via ``__RELABEL_FROM_FILE__``.
    ]]></help>
</tool>
